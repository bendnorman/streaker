class Fish extends PackMember {

  constructor(ix, iy) {
    super(ix,iy);
  }

  // new_position(average_distance, average_position) {
  //   // Vector2D a_moved = a.add(b.subtract(a).norm().multiply(d));
  //
  //   if this.pos.dist(average_position) > average_distance {
  //     weight = 1
  //   }
  //   else {
  //     weight = 2
  //   }
  //   this.pos = this.pos.add(average_position.sub(this.pos).normalize().mult(weight))
  // }

}
